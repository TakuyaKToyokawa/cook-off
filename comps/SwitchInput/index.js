import React from "react";
import styled, { css } from "styled-components";
import Switch from "react-switch";

const SwitchInput = ({}) => {
  return <Switch backgroundColorChecked="#F14B4A" />;
};

SwitchInput.defaultProps = {};

export default SwitchInput;
